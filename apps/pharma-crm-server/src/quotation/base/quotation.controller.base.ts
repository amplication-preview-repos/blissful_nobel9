/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { QuotationService } from "../quotation.service";
import { QuotationCreateInput } from "./QuotationCreateInput";
import { Quotation } from "./Quotation";
import { QuotationFindManyArgs } from "./QuotationFindManyArgs";
import { QuotationWhereUniqueInput } from "./QuotationWhereUniqueInput";
import { QuotationUpdateInput } from "./QuotationUpdateInput";
import { PiRecordFindManyArgs } from "../../piRecord/base/PiRecordFindManyArgs";
import { PiRecord } from "../../piRecord/base/PiRecord";
import { PiRecordWhereUniqueInput } from "../../piRecord/base/PiRecordWhereUniqueInput";

export class QuotationControllerBase {
  constructor(protected readonly service: QuotationService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Quotation })
  async createQuotation(
    @common.Body() data: QuotationCreateInput
  ): Promise<Quotation> {
    return await this.service.createQuotation({
      data: {
        ...data,

        product: data.product
          ? {
              connect: data.product,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,

        product: {
          select: {
            id: true,
          },
        },

        quantity: true,
        quotedPrice: true,
        updatedAt: true,
        validUntil: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Quotation] })
  @ApiNestedQuery(QuotationFindManyArgs)
  async quotations(@common.Req() request: Request): Promise<Quotation[]> {
    const args = plainToClass(QuotationFindManyArgs, request.query);
    return this.service.quotations({
      ...args,
      select: {
        createdAt: true,
        id: true,

        product: {
          select: {
            id: true,
          },
        },

        quantity: true,
        quotedPrice: true,
        updatedAt: true,
        validUntil: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Quotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async quotation(
    @common.Param() params: QuotationWhereUniqueInput
  ): Promise<Quotation | null> {
    const result = await this.service.quotation({
      where: params,
      select: {
        createdAt: true,
        id: true,

        product: {
          select: {
            id: true,
          },
        },

        quantity: true,
        quotedPrice: true,
        updatedAt: true,
        validUntil: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Quotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateQuotation(
    @common.Param() params: QuotationWhereUniqueInput,
    @common.Body() data: QuotationUpdateInput
  ): Promise<Quotation | null> {
    try {
      return await this.service.updateQuotation({
        where: params,
        data: {
          ...data,

          product: data.product
            ? {
                connect: data.product,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,

          product: {
            select: {
              id: true,
            },
          },

          quantity: true,
          quotedPrice: true,
          updatedAt: true,
          validUntil: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Quotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteQuotation(
    @common.Param() params: QuotationWhereUniqueInput
  ): Promise<Quotation | null> {
    try {
      return await this.service.deleteQuotation({
        where: params,
        select: {
          createdAt: true,
          id: true,

          product: {
            select: {
              id: true,
            },
          },

          quantity: true,
          quotedPrice: true,
          updatedAt: true,
          validUntil: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/piRecords")
  @ApiNestedQuery(PiRecordFindManyArgs)
  async findPiRecords(
    @common.Req() request: Request,
    @common.Param() params: QuotationWhereUniqueInput
  ): Promise<PiRecord[]> {
    const query = plainToClass(PiRecordFindManyArgs, request.query);
    const results = await this.service.findPiRecords(params.id, {
      ...query,
      select: {
        createdAt: true,
        date: true,
        id: true,
        issuedBy: true,

        quotation: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/piRecords")
  async connectPiRecords(
    @common.Param() params: QuotationWhereUniqueInput,
    @common.Body() body: PiRecordWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      piRecords: {
        connect: body,
      },
    };
    await this.service.updateQuotation({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/piRecords")
  async updatePiRecords(
    @common.Param() params: QuotationWhereUniqueInput,
    @common.Body() body: PiRecordWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      piRecords: {
        set: body,
      },
    };
    await this.service.updateQuotation({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/piRecords")
  async disconnectPiRecords(
    @common.Param() params: QuotationWhereUniqueInput,
    @common.Body() body: PiRecordWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      piRecords: {
        disconnect: body,
      },
    };
    await this.service.updateQuotation({
      where: params,
      data,
      select: { id: true },
    });
  }
}
